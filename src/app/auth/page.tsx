import { UserAuthForm } from '@/lib/pages/auth';

const Page = () => {
  return (
    <div className="mx-auto my-auto flex w-full max-w-xs items-center justify-center">
      <UserAuthForm />
    </div>
  );
};

export default Page;
